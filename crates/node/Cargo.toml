[package]
name = "shell-node"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
shell-primitives.workspace = true
shell-crypto.workspace = true
shell-core.workspace = true
shell-storage.workspace = true
shell-genesis = { path = "../genesis" }
shell-stark-prover = { path = "../stark-prover" }
shell-consensus = { path = "../consensus" }
shell-evm = { path = "../evm" }
shell-mempool = { path = "../mempool" }
shell-rpc = { path = "../rpc" }
shell-network = { path = "../network" }
alloy-rlp.workspace = true
tokio.workspace = true
parking_lot.workspace = true
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
thiserror.workspace = true
tracing.workspace = true
prometheus.workspace = true
hyper.workspace = true
hyper-util.workspace = true
http-body-util.workspace = true

[dev-dependencies]

[features]
# H-1: stub-l2-verifier allows L2 recursive proof verification to soft-pass.
# This MUST NOT be enabled in production. It exists only for testnet / local
# development environments where the real recursive prover is not yet wired in.
# Default builds do NOT enable this feature.
stub-l2-verifier = []
